@model IEnumerable<ContosoUni2018.Models.Student>
<!-- this is modeled by the student class -->
@{ <!-- ===================================== HTML FOR THE SITE ========================= -->
    ViewData["Title"] = "Students"; <!-- is used in the layout-->
}

<div class="container my-4>"<!-- gives margin to the container-->
<h2>Students</h2>

<p>
    <a asp-action="Create" class="btn btn-primary"><i class="fas fa-plus"></i> Create New</a>
</p>
<div class="table-responsive"><!-- makes the list scrollable to left of right-->
    <table id="tablesorted" class="table table-bordered">
        <thead class="thead-dark">
            <tr>

@*<th>
        @Html.DisplayNameFor(model => model.FirstName)
    </th><!-- this displays the header as FullName instead of firstname and last name
    <th>
        @Html.DisplayNameFor(model => model.LastName)
    </th>*@
                <th>
<!-- combine first and last names
    and the html.displaynamefor..etc if done by scalfolding
    and you can just hard code Full Name instead of the code below-->
@Html.DisplayNameFor(model => model.FullName)
                </th>
                <th>
@Html.DisplayNameFor(model => model.Email)
                </th>
                <th>
@Html.DisplayNameFor(model => model.EnrollmentDate)
                </th>
@*<th>
        @Html.DisplayNameFor(model => model.Address)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.City)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.PostalCode)
    </th>
    <th>
        @Html.DisplayNameFor(model => model.Province)
    </th>*@
                <th></th><!-- a empty header that holds the buttons (postalcode, address, city..etc)-->
            </tr>
        </thead>
        <tbody>
@foreach (var item in Model)
{
    <tr>

    @*<td><!-- these two displays the name of the person -->
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>*@
    <th>
    <!-- combine first and last names-->
    @Html.DisplayNameFor(model => model.FullName)
    </th>
    <td>
    @Html.DisplayFor(modelItem => item.Email)
    </td>
    <td>
    @Html.DisplayFor(modelItem => item.EnrollmentDate)
    </td>
    @*<td>
            @Html.DisplayFor(modelItem => item.Address)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostalCode)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Province)
        </td>*@
    <td>
    <!-- the edit, details and delete button that shows on the site and ID looks for
        the PK from the database-->
    <td class="ico text_nowrap">                                                           <!--the i class is for the font awesome icons-->
        <a class="ico-info" title="Student Details" asp-action="Details" asp-route-id="@item.ID"><i class="fas fa-search-plus"></i></a>
        <a class="ico-success" title="Edit Student" asp-action="Edit" asp-route-id="@item.ID"><i class="fas fa-edit"></i></a>
        <a class="ico-danger" title="Delete Student" asp-action="Delete" asp-route-id="@item.ID"><i class="fas fa-trash"></i></a>
    </td>
    </tr>
}
        </tbody>
    </table>
</div>